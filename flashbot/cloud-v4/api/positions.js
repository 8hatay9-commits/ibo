import{rpc,hexInt,accountData}from'../lib/base.js';
import{userPositions}from'../lib/aave.js';
export default async function handler(req,res){const observed_at=new Date().toISOString();try{const user=String(req.query?.user||'').toLowerCase();if(!/^0x[0-9a-f]{40}$/.test(user))return res.status(400).json({ok:false,error:'valid ?user=0x... required'});const h=await rpc('eth_blockNumber');const block=hexInt(h.result),blockTag='0x'+block.toString(16);const[acct,pos]=await Promise.all([accountData(user,blockTag),userPositions(user,blockTag)]);res.setHeader('cache-control','no-store');res.status(200).json({ok:true,agent:'FLASHBOT-CLOUD-V4',evidence_level:'ONCHAIN_SINGLE_BLOCK',head_block:block,account:acct,...pos,observed_at})}catch(e){res.status(502).json({ok:false,error:String(e?.message||e),details:e?.details||null,observed_at})}}
